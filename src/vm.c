#include "../include/op.h"

int main(int argc, char **argv)
{
    // parse arguments
        // if args < 2, print usage instructions
        // if args > 4, print usage instructions
        // else loop through args
            // if arg is not a .txt file, print usage instructions ??
    // create champion from each file argument
    // store champion in core
    // run core
    // print winner
    return 0;
}